{
  "name": "macroslow",
  "version": "1.0.0",
  "description": "Library of quantum resources, templates & guides",
  "main": "index.js",
  "scripts": {
    "yaml:lint": "yaml-lint **/*.yml **/*.yaml --ignore=node_modules/**"
  },
  "devDependencies": {
    "yaml-lint": "^1.7.0"
  }
}
