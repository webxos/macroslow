```yaml
# prometheus_config.yaml
# Purpose: Configures Prometheus for monitoring the CHIMERA 2048 OEM server.
# Customization: Add additional scrape targets or adjust intervals.

global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'chimera_hub'
    static_configs:
      - targets: ['localhost:9090']

# To use: Ensure Prometheus is running and pointed to this config file
# Customization: Add labels or metrics relabeling for advanced monitoring
```