{
  "mcpServers": {
    "rube-app": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-rube",
        "--token", "${RUBE_TOKEN}"
      ],
      "env": {
        // Additional required env vars for the Rube server
      }
    }
    // Define other MCP servers (e.g., for filesystem, git) as needed
  }
}m
