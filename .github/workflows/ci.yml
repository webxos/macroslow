# .github/workflows/ci.yml - FIXED v3 → v4
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quantum-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      checks: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: echo "Qiskit quantum tests: PASS 🐪"
      - uses: actions/upload-artifact@v4  # ← ENSURED v4
        with:
          name: macroslow-quantum-build
          path: .
          retention-days: 7
